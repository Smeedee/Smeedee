<UserControl x:Class="APD.Client.Widget.Admin.SL.Views.GenericConfigurationView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" 
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" 
    xmlns:data="clr-namespace:System.Windows.Controls;assembly=System.Windows.Controls.Data"
    Width="Auto" Height="Auto">
    <Grid x:Name="LayoutRoot">
        <data:DataGrid Margin="0,0,0,48" 
                       AutoGenerateColumns="False" 
                       ItemsSource="{Binding Data, Mode=TwoWay}"
                       Width="Auto"
                       Height="Auto"
                       HorizontalAlignment="Stretch"
                       VerticalAlignment="Stretch">
            <data:DataGrid.Columns>
                <data:DataGridTextColumn Header="Key" Binding="{Binding Name, Mode=TwoWay}" IsReadOnly="True"/>
                <data:DataGridTextColumn Header="Value" Binding="{Binding Value, Mode=TwoWay}" IsReadOnly="False"/>
            </data:DataGrid.Columns>
        </data:DataGrid>

    </Grid>
</UserControl>

<UserControl x:Class="Smeedee.Widgets.SL.GenericCharting.Views.ChartView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:charting="clr-namespace:System.Windows.Controls.DataVisualization.Charting;assembly=System.Windows.Controls.DataVisualization.Toolkit"
             xmlns:local="clr-namespace:Smeedee.Widgets.SL.GenericCharting.Views" mc:Ignorable="d"
             d:DesignHeight="300"
             d:DesignWidth="400">

    <Grid x:Name="LayoutRoot">
    	<local:GenericChart Title="{Binding Name, Mode=OneWay}" LinesSource="{Binding Lines, Mode=OneWay}">
            <local:GenericChart.LineTemplate>
                <DataTemplate>
                    <charting:LineSeries ItemsSource="{Binding Data}" Title="{Binding Name}" DependentValuePath="Y" IndependentValuePath="X" />
                </DataTemplate>
            </local:GenericChart.LineTemplate>
    	</local:GenericChart>

        <!--
        <Charting:Chart Title="{Binding ChartName}"
                        ChartAreaStyle="{}"
                        LegendStyle="{}"
                        PlotAreaStyle="{}"
                        Name="Chart">



            Just testing some code and functionality
            
            <Charting:LineSeries Title="FirstLine"
                                 ItemsSource="{Binding a}"
                                 IndependentValueBinding="{Binding TeaCupsDrunk}"
                                 DependentValueBinding="{Binding number}"
                                 Name="testGraph1" />
            <Charting:LineSeries Title="SecondLine"
                                 ItemsSource="{Binding b}"
                                 IndependentValueBinding="{Binding CoffeeCupsDrunk}"
                                 DependentValueBinding="{Binding number}"
                                 Name="testGraph2"/>


            
            
            <Charting:LineSeries
                ItemsSource="{Binding}"
                DependentValueBinding="{Binding Y}"
                IndependentValueBinding="{Binding X}" />
            <Charting:ColumnSeries
                Title="Ford"
                DataContext="{Binding PointsA}"
                ItemsSource="{Binding}"
                DependentValueBinding="{Binding Y}"
                IndependentValueBinding="{Binding X}"
                AnimationSequence="FirstToLast"
                TransitionDuration="5000" >
                <Charting:ColumnSeries.TransitionEasingFunction>
                    <ElasticEase EasingMode="EaseIn" />
                </Charting:ColumnSeries.TransitionEasingFunction>
            </Charting:ColumnSeries>
            <Charting:Chart.Axes>
                <Charting:LinearAxis x:Name="yaxis" Orientation="Y"
                    ShowGridLines="False" Title="Y Axis Values" 
                    Minimum="-100" Maximum="100" Interval="50" />
                <Charting:CategoryAxis x:Name="xaxis" Orientation="X"
                                       Title="X Axis Values" />
            </Charting:Chart.Axes> 
            <Charting:Chart.Series>
                <Charting:LineSeries
                    DataContext="{Binding PointsB}"
                    ItemsSource="{Binding}"
                    DependentValuePath="Y"
                    DependentRangeAxis="{Binding ElementName=yaxis}"
                    IndependentValuePath="X"
                    IndependentAxis="{Binding ElementName=xaxis}" />
            </Charting:Chart.Series>
            
            
        </Charting:Chart>
        -->

    </Grid>
</UserControl>